/*
 * Generated by Ansible
 * /etc/ansible/icinga2/playbooks/roles/icinga2_client/templates/satellite-fab1.j2 
 */

object Endpoint "{{ groups.icinga2master[0] }}" {
  // This endpoint will connect to us
}

object Endpoint "{{ groups.icinga2secondmaster[0] }}" {
  // This endpoint will connect to us
}

object Endpoint "{{ inventory_hostname }}" {
  // That's us
}

object Endpoint "{{ groups.icinga2satellitefab[1] }}" {
  host = "{{ hostvars[groups['icinga2satellitefab'][1]]['ansible_ens33']['ipv4']['address'] }}"
}

object Zone "master" {
        endpoints = [ "{{ groups.icinga2master[0] }}", "{{ groups.icinga2secondmaster[0] }}" ]
}

object Zone "{{ icinga_zone }}" {
        endpoints = [ "{{ inventory_hostname }}", "{{ groups.icinga2satellitefab[1] }}" ]
        parent = "master"
}

object Zone "global-templates" {
        global = true
}

object Zone "director-global" {
        global = true
}
